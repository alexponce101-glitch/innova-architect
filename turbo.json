{
  "$schema": "https://turbo.build/schema.json",
  "ui": "tui",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["NODE_ENV", "INNOVA_DATA_DIR"]
    },
    "lint": {
      "dependsOn": ["^lint"],
      "env": ["NODE_ENV", "INNOVA_DATA_DIR"]
    },
    "check-types": {
      "dependsOn": ["^check-types"]
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "env": ["NODE_ENV", "INNOVA_DATA_DIR"]
    }
  }
}
